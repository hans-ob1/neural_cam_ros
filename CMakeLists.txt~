cmake_minimum_required(VERSION 2.4.6)
include($ENV{ROS_ROOT}/core/rosbuild/rosbuild.cmake)

rosbuild_init()

find_package(CUDA 7.5 REQUIRED)
find_package(OpenCV 3.1 REQUIRED)

rosbuild_genmsg()

#set the default path for built executables to the "bin" directory
set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
#set the default path for built libraries to the "lib" directory
set(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/lib)

INCLUDE(FindCUDA)

INCLUDE_DIRECTORIES(/usr/local/cuda/include)

set(
    CUDA_NVCC_FLAGS
    ${CUDA_NVCC_FLAGS};
    -O3 -gencode arch=compute_61,code=sm_61
    )

#set flags
add_definitions(-DGPU -DCUDNN)

#wildcard additions:
file(GLOB SOURCES "src/*.c" "src/*.cu" "src/*.h")

cuda_add_library(neurlib ${SOURCES} OPTIONS -arch sm_61)
TARGET_LINK_LIBRARIES(neurlib m pthread stdc++ cublas curand cudnn)

rosbuild_add_executable(main src/main.cpp src/supportfunc.cpp src/supportfunc.hpp)
rosbuild_add_compile_flags(main -std=c++11)

TARGET_LINK_LIBRARIES(main ${OpenCV_LIBS} neurlib)
